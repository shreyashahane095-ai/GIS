# Theme System Implementation TODO

- [ ] Update theme initialization to avoid flicker (if needed)
- [x] Replace hardcoded colors in `src/components/controls/SearchPanel.css` with CSS variables
- [ ] Audit and replace hardcoded colors in key CSS files (Navbar, LeftSidebar, DataActionPanel, BrowseDataHub popup, OperationsPanel, Chatbot)
- [ ] Ensure hover/active/focus/borders/shadows use variables
- [ ] Add/verify smooth transitions on theme change
- [ ] Implement map light/dark tile switching (if tile provider exists)
- [ ] Manual verification: persistence, contrast, responsiveness, no wrong-theme leftovers

